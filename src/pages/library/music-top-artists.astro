---
import * as fs from 'node:fs/promises';
import * as path from 'node:path';

let db: any = { lastfm: {}, listenbrainz: {} };
try {
  const raw = await fs.readFile(
    path.resolve('./src/data/generated/music.json'),
    'utf-8',
  );
  db = JSON.parse(raw);
} catch (_e) {
  console.warn('[Music Top Artists] music.json not found.');
}

const _topArtists = db.lastfm?.topArtists || [];
const _lbArtists = db.listenbrainz?.topArtists || [];

const fallbackGradients = [
  'linear-gradient(135deg, #e11d48, #881337)',
  'linear-gradient(135deg, #1e3a5f, #7c3aed)',
  'linear-gradient(135deg, #d97706, #b91c1c)',
  'linear-gradient(135deg, #7c3aed, #db2777)',
  'linear-gradient(135deg, #0c4a6e, #1e40af)',
  'linear-gradient(135deg, #065f46, #047857)',
  'linear-gradient(135deg, #365314, #3f6212)',
];
const fallbackEmojis = ['🎵', '🎶', '🎤', '🎧', '🎸', '🎹', '🥁'];

function _getVisuals(name: string) {
  if (!name)
    return { gradient: fallbackGradients[0], emoji: fallbackEmojis[0] };
  const idx = [...name].reduce((acc, char) => acc + char.charCodeAt(0), 0);
  return {
    gradient: fallbackGradients[idx % fallbackGradients.length],
    emoji: fallbackEmojis[idx % fallbackEmojis.length],
  };
}
---

<Layout title="Top Artists" description="Chirag Singhal's top artists from Last.fm and ListenBrainz">
  <Navbar />
  <CommandPalette />
  <main class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8 py-12">
    <a href="/library/music" class="inline-flex items-center gap-2 text-sm text-[var(--color-text-muted)] hover:text-[var(--color-text-secondary)] mb-8 transition-colors">
      <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M15 19l-7-7 7-7" /></svg>
      Back to Music
    </a>

    <div class="flex items-center gap-3 mb-2">
      <div class="h-10 w-10 rounded-xl bg-[var(--color-accent-rose)]/10 flex items-center justify-center">
        <span class="text-xl">🎤</span>
      </div>
      <h1 class="text-4xl font-bold font-[var(--font-display)] text-white">Top Artists</h1>
    </div>
    <p class="text-[var(--color-text-secondary)] mb-10">Most listened artists — from Last.fm weekly data.</p>

    <!-- Last.fm Top Artists -->
    <section class="mb-16">
      <h2 class="section-heading text-xl font-semibold text-white mb-8">Last.fm — Top Artists (7 Days)</h2>
      {topArtists.length > 0 ? (
        <div class="grid grid-cols-2 sm:grid-cols-4 md:grid-cols-5 gap-4">
          {topArtists.map((artist: any, i: number) => {
            const vis = getVisuals(artist.name);
            return (
            <a href={artist.url} target="_blank" rel="noopener noreferrer" class="card group cursor-default text-center block" style={`animation-delay: ${i * 40}ms`}>
              <div class="w-20 h-20 mx-auto rounded-full mb-3 flex items-center justify-center text-3xl relative overflow-hidden" style={`background: ${vis.gradient}`}>
                <div class="absolute inset-0 bg-black/10"></div>
                {artist.imageUrl ? (
                  <img src={artist.imageUrl} alt={artist.name} class="w-full h-full object-cover relative z-10" />
                ) : (
                  <span class="relative z-10 drop-shadow-lg">{vis.emoji}</span>
                )}
              </div>
              <h3 class="text-xs font-semibold text-white truncate group-hover:text-[var(--color-accent-rose)] transition-colors">{artist.name}</h3>
              <span class="text-[0.65rem] text-[var(--color-accent-rose)] font-medium">{artist.playcount.toLocaleString()} plays</span>
            </a>
          )})}
        </div>
      ) : (
        <div class="glass p-8 text-center"><p class="text-[var(--color-text-muted)]">No Last.fm data available.</p></div>
      )}
    </section>

    <!-- ListenBrainz Top Artists -->
    {lbArtists.length > 0 && (
      <section class="mb-16">
        <div class="divider-warm mb-16"></div>
        <h2 class="section-heading text-xl font-semibold text-white mb-8">ListenBrainz — Top Artists</h2>
        <div class="grid grid-cols-2 sm:grid-cols-4 md:grid-cols-5 gap-4">
          {lbArtists.map((artist: any, i: number) => {
            const vis = getVisuals(artist.name);
            return (
            <div class="card group cursor-default text-center" style={`animation-delay: ${i * 40}ms`}>
              <div class="w-20 h-20 mx-auto rounded-full mb-3 flex items-center justify-center text-3xl relative overflow-hidden" style={`background: ${vis.gradient}`}>
                <div class="absolute inset-0 bg-black/10"></div>
                <span class="relative z-10 drop-shadow-lg">{vis.emoji}</span>
              </div>
              <h3 class="text-xs font-semibold text-white truncate group-hover:text-[var(--color-accent-warm)] transition-colors">{artist.name}</h3>
              <span class="text-[0.65rem] text-[var(--color-accent-warm)] font-medium">{artist.listenCount} listens</span>
            </div>
          )})}
        </div>
      </section>
    )}
  </main>
  <Footer />
</Layout>
