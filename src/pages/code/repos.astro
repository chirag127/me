---
import Layout from '../../layouts/Layout.astro';
import CommandPalette from '../../components/CommandPalette.astro';

import { fetchGitHubRepos } from '../../lib/api';

const repos = await fetchGitHubRepos();
const sortedRepos = repos.sort(
  (a, b) => b.stargazers_count - a.stargazers_count,
);
const languages = [
  ...new Set(repos.filter((r) => r.language).map((r) => r.language)),
].sort();
const totalStars = repos.reduce((sum, r) => sum + r.stargazers_count, 0);
const totalForks = repos.reduce((sum, r) => sum + r.forks_count, 0);
---

<Layout title="Repositories" description={`${sortedRepos.length} public GitHub repositories by Chirag Singhal`}>
  <CommandPalette />
  <main class="mx-auto max-w-[var(--content-max-width)] px-6 py-12 relative">
    <!-- Header -->
    <div class="mb-10 relative reveal">
      <div class="inline-flex items-center gap-2 px-3 py-1.5 rounded-full bg-teal-500/10 border border-teal-500/20 text-[11px] font-semibold text-teal-400 uppercase tracking-wider mb-5">
        <span class="h-1.5 w-1.5 rounded-full bg-teal-400 animate-pulse"></span>
        CODE
      </div>
      <h1 class="text-3xl sm:text-4xl font-black text-white mb-2" style="font-family: var(--font-display, 'Outfit');">Repositories</h1>
      <p class="text-base text-white/40">{sortedRepos.length} repos · {totalStars} stars · {totalForks} forks · {languages.length} languages</p>
    </div>

    <!-- Search Input -->
    <div class="mb-6 reveal">
      <div class="relative">
        <svg class="absolute left-4 top-1/2 -translate-y-1/2 h-4 w-4 text-white/30 pointer-events-none" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><circle cx="11" cy="11" r="8" /><path d="m21 21-4.3-4.3" /></svg>
        <input
          type="text"
          id="repo-search"
          placeholder="Search repos by name or description..."
          class="w-full pl-10 pr-4 py-3 rounded-xl bg-white/[0.03] border border-white/5 text-sm text-white placeholder:text-white/30 focus:outline-none focus:border-teal-500/30 focus:bg-white/[0.05] transition-all"
        />
      </div>
    </div>

    <!-- Filter / Sort Chips -->
    <div class="flex flex-wrap gap-3 mb-8 reveal">
      <select
        id="lang-filter"
        class="px-4 py-2 rounded-lg bg-white/[0.03] border border-white/5 text-white text-sm focus:outline-none focus:border-teal-500/30 transition-colors cursor-pointer"
      >
        <option value="all" class="bg-gray-900">All Languages ({languages.length})</option>
        {languages.map((lang) => (<option value={lang} class="bg-gray-900">{lang}</option>))}
      </select>
      <select
        id="sort-filter"
        class="px-4 py-2 rounded-lg bg-white/[0.03] border border-white/5 text-white text-sm focus:outline-none focus:border-teal-500/30 transition-colors cursor-pointer"
      >
        <option value="stars" class="bg-gray-900">Most Stars</option>
        <option value="forks" class="bg-gray-900">Most Forks</option>
        <option value="updated" class="bg-gray-900">Recently Updated</option>
        <option value="name" class="bg-gray-900">Name A-Z</option>
      </select>
    </div>

    <!-- Repo Count -->
    <div class="text-sm text-white/30 mb-4" id="repo-count">Showing {sortedRepos.length} repos</div>

    <!-- Repos Grid -->
    <div id="repos-grid" class="grid grid-cols-1 md:grid-cols-2 gap-4">
      {sortedRepos.map((repo) => (
        <div
          class="glass p-5 group cursor-pointer"
          data-lang={repo.language || 'unknown'}
          data-stars={repo.stargazers_count}
          data-forks={repo.forks_count}
          data-updated={repo.updated_at}
          data-name={repo.name.toLowerCase()}
        >
          <div class="flex items-start justify-between mb-2">
            <h3 class="text-sm font-semibold text-white group-hover:text-teal-400 transition-colors truncate mr-3">{repo.name}</h3>
            <div class="flex items-center gap-3 text-xs text-white/30 flex-shrink-0">
              {repo.stargazers_count > 0 && (
                <span class="flex items-center gap-1">
                  <svg class="h-3.5 w-3.5" fill="currentColor" viewBox="0 0 16 16"><path d="M8 .25a.75.75 0 01.673.418l1.882 3.815 4.21.612a.75.75 0 01.416 1.279l-3.046 2.97.719 4.192a.75.75 0 01-1.088.791L8 12.347l-3.766 1.98a.75.75 0 01-1.088-.79l.72-4.194L.818 6.374a.75.75 0 01.416-1.28l4.21-.611L7.327.668A.75.75 0 018 .25z"/></svg>
                  {repo.stargazers_count}
                </span>
              )}
              {repo.forks_count > 0 && (
                <span class="flex items-center gap-1">
                  <svg class="h-3.5 w-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M8.684 13.342C8.886 12.938 9 12.482 9 12c0-.482-.114-.938-.316-1.342m0 2.684a3 3 0 110-2.684m0 2.684l6.632 3.316m-6.632-6l6.632-3.316m0 0a3 3 0 105.367-2.684 3 3 0 00-5.367 2.684zm0 9.316a3 3 0 105.368 2.684 3 3 0 00-5.368-2.684z" /></svg>
                  {repo.forks_count}
                </span>
              )}
            </div>
          </div>
          {repo.description && <p class="text-xs text-white/40 mb-3 line-clamp-2">{repo.description}</p>}
          <div class="flex items-center gap-2 flex-wrap">
            {repo.language && (
              <span class="inline-flex items-center gap-1.5 text-xs text-white/50">
                <span class="h-2 w-2 rounded-full bg-teal-400"></span>
                {repo.language}
              </span>
            )}
            {repo.topics?.slice(0, 4).map((topic) => (
              <span class="px-2 py-0.5 text-[10px] rounded-full bg-white/5 text-white/30 border border-white/5">{topic}</span>
            ))}
          </div>
        </div>
      ))}
    </div>

    <!-- Empty State -->
    <div id="empty-state" class="hidden text-center py-16">
      <div class="text-4xl mb-3">🔍</div>
      <h3 class="text-lg font-semibold text-white mb-1">No repos found</h3>
      <p class="text-sm text-white/40">Try a different search or filter</p>
    </div>
  </main>
  <script is:inline>
    const searchInput = document.getElementById('repo-search');
    const langFilter = document.getElementById('lang-filter');
    const sortFilter = document.getElementById('sort-filter');
    const grid = document.getElementById('repos-grid');
    const repoCount = document.getElementById('repo-count');
    const emptyState = document.getElementById('empty-state');

    function filterRepos() {
      const search = searchInput?.value.toLowerCase() || '';
      const lang = langFilter?.value || 'all';
      const sort = sortFilter?.value || 'stars';

      const cards = [...document.querySelectorAll('.repo-card, .glass[data-lang]')];
      let visible = 0;

      cards.forEach(card => {
        const name = card.dataset.name || '';
        const cardLang = card.dataset.lang || '';
        const desc = card.textContent?.toLowerCase() || '';
        const matchesSearch = !search || name.includes(search) || desc.includes(search);
        const matchesLang = lang === 'all' || cardLang === lang;
        const show = matchesSearch && matchesLang;
        card.style.display = show ? 'block' : 'none';
        if (show) visible++;
      });

      if (repoCount) repoCount.textContent = `Showing ${visible} repos`;
      if (emptyState) emptyState.classList.toggle('hidden', visible > 0);

      const visibleCards = cards.filter(c => c.style.display !== 'none');
      visibleCards.sort((a, b) => {
        if (sort === 'stars') return parseInt(b.dataset.stars || '0') - parseInt(a.dataset.stars || '0');
        if (sort === 'forks') return parseInt(b.dataset.forks || '0') - parseInt(a.dataset.forks || '0');
        if (sort === 'updated') return (b.dataset.updated || '').localeCompare(a.dataset.updated || '');
        return (a.dataset.name || '').localeCompare(b.dataset.name || '');
      });
      visibleCards.forEach(card => grid?.appendChild(card));
    }

    searchInput?.addEventListener('input', filterRepos);
    langFilter?.addEventListener('change', filterRepos);
    sortFilter?.addEventListener('change', filterRepos);
  </script>
</Layout>
