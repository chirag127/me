# Certificates — public files
Drop cert PDFs/images here (e.g. sap-abap-cloud.pdf), then reference them in
src/pages/certificates/index.astro via file: '/certificates/<name>.pdf'.
The employer-trusted anchor is each cert's official verifyUrl, not the PDF.
